apiVersion: v1
kind: Secret
metadata:
  name: taskboard-env
  namespace: taskboard
type: Opaque
stringData:
  # Demo-only — encryption-in-repo lands in Phase 6.
  # User/pass/db all "taskboard" — mirrors postgres-credentials in the data ns.
  DATABASE_URL: postgresql://taskboard:taskboard@postgres.data.svc.cluster.local:5432/taskboard
